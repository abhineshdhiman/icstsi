# Running ICSTSI with Docker

```bash
cp .env.example .env
# fill in real credentials in .env
docker compose up
```

Web preview at http://localhost:8080. iOS/Android still require local Flutter SDK.
